/// Name this plugin reports to the Rivium backend as its SDK identity.
const String riviumPushSdkName = 'flutter';

/// Version of the rivium_push plugin.
///
/// Must match `version` in pubspec.yaml (enforced by a unit test). Reported
/// to the backend as the SDK version on registration.
const String riviumPushSdkVersion = '0.1.17';
